import { library } from '@fortawesome/fontawesome-svg-core';

import {
    faShoppingCart,
    faThumbsUp,
    faChevronRight,
    faTrash,
} from '@fortawesome/free-solid-svg-icons';

library.add(
    faShoppingCart,
    faThumbsUp,
    faChevronRight,
    faTrash,
);